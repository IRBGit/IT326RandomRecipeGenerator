# IT326RandomRecipeGenerator

All Java files are depreciated as we have switched to Python.

This project is a student project designed to be a database for recipes.

Project dependencies:
For password hashing: pip install argon2-cffi
for database security: pip install python-dotenv
for the recipe to pdf use case: pip install json2pdf_converter
for database managment: SQLAlchemy